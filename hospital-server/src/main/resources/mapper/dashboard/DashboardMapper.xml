<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.cloudhospital.dashboard.DashboardMapper">
  <select id="countPatients" resultType="long">SELECT COUNT(*) FROM patient</select>
  <select id="countEnabledDoctors" resultType="long">SELECT COUNT(*) FROM doctor WHERE enabled = b'1'</select>
  <select id="countRegistrationsByStatus" resultType="com.example.cloudhospital.dashboard.DashboardController$CountByStatus">SELECT status, COUNT(*) AS total FROM registration WHERE visit_date = #{visitDate} GROUP BY status</select>
  <select id="countPrescriptionsByStatus" resultType="com.example.cloudhospital.dashboard.DashboardController$CountByStatus">SELECT status, COUNT(*) AS total FROM prescription GROUP BY status</select>
  <select id="sumUnpaidAmount" resultType="java.math.BigDecimal">SELECT COALESCE(SUM(total_amount), 0) FROM prescription WHERE status = 'UNPAID'</select>
</mapper>
