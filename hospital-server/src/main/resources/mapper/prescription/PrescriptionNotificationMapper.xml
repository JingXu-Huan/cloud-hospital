<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.cloudhospital.prescription.PrescriptionNotificationMapper">
  <select id="countPatientAccounts" resultType="int">SELECT COUNT(*) FROM user_account WHERE patient_id = #{patientId} AND role = 'PATIENT'</select>
  <insert id="insertPickupReadyNotification">INSERT INTO patient_notification (patient_id, prescription_id, type, title, content) VALUES (#{patientId}, #{prescriptionId}, 'PICKUP_READY', '您的药品已配好', CONCAT('处方 ', #{prescriptionNo}, ' 已完成发药，请前往药房窗口取药。'))</insert>
  <update id="markNotificationsReadByPrescriptionId">UPDATE patient_notification SET read_at = COALESCE(read_at, NOW()) WHERE prescription_id = #{prescriptionId}</update>
</mapper>
